# Serveur d'envoi d'emails (server/) — utilisé quand vous lancez "npm start" dans server/
# Gmail : créer un "mot de passe d'application" dans le compte Google
EMAIL_USER=marthedufour23@gmail.com
EMAIL_PASS=votre_mot_de_passe_application

# Optionnel : URL du serveur (défaut http://localhost:3001)
# VITE_API_URL=http://localhost:3001
